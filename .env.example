# Copy this file to .env and fill in real values. .env is gitignored.

# --- Local Postgres (docker-compose "db" service) ---
POSTGRES_USER=trailwise
POSTGRES_PASSWORD=change_me_dev_password
POSTGRES_DB=trailwise
POSTGRES_PORT=5432

# --- Backend connection string ---
# Local docker-compose Postgres (default):
CONNECTION_STRING=Host=db;Port=5432;Database=trailwise;Username=trailwise;Password=change_me_dev_password
# To point the stack at a Supabase cloud Postgres project instead, replace the above with the
# connection string from Supabase (Project Settings -> Database -> Connection string -> .NET),
# e.g.: Host=aws-0-xx.pooler.supabase.com;Port=5432;Database=postgres;Username=postgres.xxxx;Password=...;SSL Mode=Require;Trust Server Certificate=true

# --- JWT ---
JWT_SECRET=change_me_to_a_long_random_string_at_least_32_chars
JWT_ISSUER=TrailWise
JWT_AUDIENCE=TrailWiseClients
JWT_EXPIRY_MINUTES=60

# --- Seeded Admin account (created on first backend startup if it doesn't already exist) ---
ADMIN_SEED_NAME=System Admin
ADMIN_SEED_EMAIL=admin@trailwise.local
ADMIN_SEED_PASSWORD=change_me_admin_password

# --- CORS ---
CORS_ALLOWED_ORIGIN=http://localhost:5173

# --- Backend port exposed to the host ---
BACKEND_PORT=5080

# --- React dev server ---
FRONTEND_WEB_PORT=5173
VITE_API_BASE_URL=http://localhost:5080

# --- Flutter (run natively, not via docker-compose) ---
# flutter run --dart-define=API_BASE_URL=http://localhost:5080
